// Logical Conditions 1| Predict the output of the following code:

if (false || true) {
  console.log('Yes!');
} else {
  console.log('No...');
}

// Output = 'yes'. The logical or operator is used, meaning that it will evaluate to truthy if either of it's operands is truthy.