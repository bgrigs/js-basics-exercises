// Type | How can you check whether a variable holds a value that is an array? For example, imagine you start writing a function and want to check whether its argument is an array:

let test1 = [1, 2];
let test2 = 11;

console.log(Array.isArray(test1));
console.log(Array.isArray(test2));
