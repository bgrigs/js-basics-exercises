// What will the following code log to the console and why? Don't run it until you have tried to answer.

if (true) {
  let myValue = 20;
}

console.log(myValue);

// myValue is not defined. Variables declared with let are block scoped. In order to log the value of myValue to the console, myValue would have to be initialized with outside of the if block. 